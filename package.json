{
  "name": "React-Native-MulitSelection",
  "version": "0.0.8",
  "description": "A Cross-Platform (iOS / Android) single and multiple selection React Native component.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wonderfulmobileworld/React-Native-MulitSelection.git"
  }
}
